# Strategy-Switch PWA — Cloud Dependencies (Oracle Cloud / Linux)
flask>=3.1
gunicorn>=23.0
pandas>=2.1
numpy>=1.26
yfinance>=0.2.40
tqdm>=4.66
scipy>=1.11

# Optional: IBKR Auto-Trading (nur lokal, nicht in GitHub Actions)
# ib_insync>=0.9
